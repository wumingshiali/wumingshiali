            - name: WakaTime Snake Animation
  # You may pin to the exact commit or the version.
  # uses: sandeepyadav1478/snk-wakatime@633c3ac958843d5e80db56c2f2ffbf138be180df
  uses: sandeepyadav1478/snk-wakatime@v1.0.3
  with:
    # Public WakaTime JSON share URL for coding activity data.
Create one at https://wakatime.com/settings/shares
Example: https://wakatime.com/share/@username/uuid.json

    wakatime_json_url: # optional
    # GitHub username to read contribution graph from. Only needed when wakatime_json_url is not provided, or as fallback.
    github_user_name: # optional
    # GitHub token used to fetch the contribution calendar. Defaults to the action token.
    github_token: # optional, default is ${{ github.token }}
    # list of files to generate.
one file per line. Each output can be customized with options as query string.

supported query string options:

- palette:      A preset of color, one of [github, github-dark, github-light]
- color_snake:  Color of the snake
- color_dots:   Coma separated list of dots color.
                The first one is 0 contribution, then it goes from the low contribution to the highest.
                Exactly 5 colors are expected.
example:
  outputs: |
    dark.svg?palette=github-dark&color_snake=blue
    light.svg?color_snake=#7845ab
    ocean.gif?color_snake=orange&color_dots=#bfd6f6,#8dbdff,#64a1f4,#4b91f1,#3c7dd9

    outputs: 
          
